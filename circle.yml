general:
  branches:
    only:
      - master
      - develop
deployment:
  production:
    branch: master
    commands:
      - heroku run rake db:migrate --app agile-dusk-1843
      - heroku ps:restart --app agile-dusk-1843
