general:
  branches:
    only:
      - master
      - develop
deployment:
  production:
    branch: master
    commands:
       - heroku run rake db:migrate --app peaseful-shore-2090
       - heroku ps:restart --app peaseful-shore-2090
